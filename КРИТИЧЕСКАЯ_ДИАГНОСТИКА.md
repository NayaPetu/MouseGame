# КРИТИЧЕСКАЯ ПРОБЛЕМА: Start() не вызывается + загружается wrong сцена!

## Проблема:
1. ❌ `ImageCutscene.Start()` НЕ вызывается (есть только `Awake` и `OnEnable`)
2. ❌ GameManager.OnSceneLoaded получает "main" вместо "IntroCutscene"
3. ❌ Сцена сразу переходит к main после загрузки IntroCutscene

## ГЛАВНАЯ ПРИЧИНА:

### 🚨 КОНФЛИКТ МЕЖДУ MainMenuUI И MenuButtons!
**Оба скрипта настраивают одну и ту же кнопку "Начать", и оба метода вызываются при клике!**

- `MainMenuUI.OnStartGame()` → загружает `main` напрямую
- `MenuButtons.StartGame()` → должен загружать `IntroCutscene`

Когда вы нажимаете кнопку, вызываются **ОБА** метода, и один из них перезагружает сцену!

## Что я исправил:

1. ✅ **MenuButtons.cs**: Теперь удаляет ВСЕ слушатели перед добавлением нового
2. ✅ **MenuButtons.cs**: Удаляет слушатели также в методе StartGame() перед загрузкой сцены
3. ✅ **MenuButtons.cs**: Использует `LoadSceneMode.Single` для гарантии выгрузки предыдущей сцены
4. ✅ **ImageCutscene.cs**: Добавлена проверка сцены в Start() и более подробное логирование
5. ✅ **GameManager.cs**: Добавлен стек вызовов для отладки
6. ✅ **MainMenuUI.cs**: Нужно отключить настройку кнопки, если MenuButtons присутствует (см. ниже)

## ⚠️ КРИТИЧЕСКИ ВАЖНО: ЧТО НУЖНО СДЕЛАТЬ В UNITY:

### Шаг 1: Проверьте сцену `menu`
1. Откройте сцену `menu` в Unity
2. Найдите GameObject с компонентом `MainMenuUI`
3. **Вариант А (рекомендуется)**: Отключите компонент `MainMenuUI` или удалите его
4. **Вариант Б**: Оставьте только компонент `MenuButtons` активным

### Шаг 2: Убедитесь, что кнопка настроена правильно
1. Найдите GameObject кнопки "Начать" (Button_start)
2. В Inspector проверьте компонент Button
3. Убедитесь, что в разделе "On Click ()" нет дублирующихся слушателей
4. Если там есть слушатель от MainMenuUI, удалите его вручную

### Шаг 3: Сохраните и протестируйте
1. **Сохраните все файлы (Ctrl+S)**
2. **Сохраните сцену menu (Ctrl+S)**
3. Запустите игру и нажмите "Начать"
4. Посмотрите в консоль Unity

## Что проверить в логах:

### ✅ Хорошие логи (должны быть):
```
[MenuButtons] StartGame ВЫЗВАН! Загружаю кат-сцену!
[MenuButtons] Удаляю ВСЕ слушатели кнопки перед загрузкой сцены!
[ImageCutscene] Awake вызван! Текущая сцена: IntroCutscene
[ImageCutscene] Start вызван! Текущая сцена: IntroCutscene
[GameManager] Загружена сцена IntroCutscene - ничего не делаю
```

### ❌ Плохие логи (означают проблему):
```
[MainMenuUI] OnStartGame ВЫЗВАН!  ← ЭТО НЕ ДОЛЖНО БЫТЬ!
[ImageCutscene] Start вызван на неправильной сцене!  ← Проблема!
[GameManager] КРИТИЧЕСКОЕ: Загружена сцена main!  ← После IntroCutscene это ошибка!
```

## Если проблема не решена:

Отправьте мне полные логи из консоли Unity, включая:
- Все сообщения с `[MenuButtons]`
- Все сообщения с `[MainMenuUI]`
- Все сообщения с `[ImageCutscene]`
- Все сообщения с `[GameManager]`
- Особенно стек вызовов из `[GameManager] КРИТИЧЕСКОЕ: Загружена сцена main!`

